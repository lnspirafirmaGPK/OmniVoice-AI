name: Legacy CI (Deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow notice
        run: echo "This workflow is deprecated. Use CI/CD/Security workflows instead."
